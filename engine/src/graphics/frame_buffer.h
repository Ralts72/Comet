#pragma once
#include "vk_common.h"

namespace Comet {
    class Device;
    class RenderPass;

    class FrameBuffer {
    public:
        // FrameBuffer(Device* device, RenderPass* render_pass);
    private:
    };
}
# Comet Engine 共享运行配置

# Vulkan 渲染设置
vulkan:
  # 表面格式: bgra8_srgb, bgra8_unorm, rgba8_srgb, rgba8_unorm
  surface_format: bgra8_srgb
  # 颜色空间
  color_space: srgb_nonlinear
  # 深度格式: d32_float, d24_unorm_s8_uint, d32_float_s8_uint
  depth_format: d32_float
  # 呈现模式: immediate, mailbox, fifo, fifo_relaxed
  present_mode: immediate
  # Swapchain 图像数量
  swapchain_image_count: 3
  # MSAA 采样数 (1, 2, 4, 8, 16, 32, 64)
  msaa_samples: 4  # Count4

# 渲染设置
render:
  # CPU 可并行准备的帧槽位数量
  max_frames_in_flight: 2
  # 清除颜色 (RGBA)
  clear_color: [ 0.2, 0.4, 0.1, 1.0 ]
  # 启用垂直同步
  enable_vsync: false
  # 期望的各向异性过滤倍率；1 表示关闭，超过设备上限时会回退到支持的最大值
  max_anisotropy: 8

# 窗口设置
window:
  width: 960
  height: 720
  title: "Comet Engine"
  fullscreen: false
  resizable: true
